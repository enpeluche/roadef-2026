// #include "common/types.hpp"

#pragma once

#include <cstdint>

// marqueurs temporels
using TickCount = uint8_t;
using Tick = uint8_t;

// marqueurs noeuds

using NodeCount = uint16_t;
using NodeId = uint16_t;